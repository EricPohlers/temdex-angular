export interface TypeResponse {
  name: string;
  icon: string;
}
export interface Type {
  name: string;
  icon: string;
  badgeColor: string;
  bgColor: string;
  toBgColor: string;
  fromBgColor: string;
}
